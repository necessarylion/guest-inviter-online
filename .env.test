SESSION_DRIVER=memory

# Database (PostgreSQL) — uses the local dev container, separate test database
DB_HOST=localhost
DB_PORT=5439
DB_USER=postgres
DB_PASSWORD=postgres
DB_DATABASE=event_inviter_test
